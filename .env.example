# Copy this file to .env for local development with `vercel dev`,
# and set the same variable in your hosting provider's dashboard for
# production (e.g. Vercel → Project → Settings → Environment Variables).
ANTHROPIC_API_KEY=sk-ant-...
